// prettier.config.mjs
export default {
    semi: true,                // Adds semicolons at the end of statements
    singleQuote: false,         // Uses single quotes instead of double quotes
    trailingComma: 'all',      // Adds trailing commas where valid in ES5 (objects, arrays, etc.)
    tabWidth: 2,               // Sets indentation to 2 spaces
    useTabs: false,            // Uses spaces for indentation instead of tabs
    printWidth: 80,            // Wraps lines at 80 characters
    bracketSpacing: true,      // Adds spaces between brackets in object literals
    endOfLine: 'lf',           // Uses LF for line endings
};